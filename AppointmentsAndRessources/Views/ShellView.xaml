<Window x:Class="AppointmentsAndRessources.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:res="clr-namespace:AppointmentsAndRessources.UserControls"
        mc:Ignorable="d"
        Title="ShellView" Height="800" Width="1200">
    <Grid>
        <Grid.Resources>
            <DataTemplate x:Key="WeekdayTemplate">
                <res:Weekday Width="Auto"/>
            </DataTemplate>
        </Grid.Resources>

        <ListView ItemsSource="{Binding Wochentage}" ItemTemplate="{StaticResource WeekdayTemplate}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"></StackPanel>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
        
    </Grid>
</Window>
